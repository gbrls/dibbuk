(struct Request ())

; NOTE: Nah I'll have to do this in Rust
;
; stubs for funcions defined on Rust
(define (GdbCommandsReq l) (Request))
(define (EmptyReq) (Request))

(require "dibbuk.scm")

; (define (Request? r))
